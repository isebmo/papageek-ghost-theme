<nav class="pagination" role="navigation">
    {{#if prev}}
        <a href="{{page_url prev}}"><i class="fa fa-chevron-left"></i></a>
    {{/if}}
    <span>{{page}} / {{pages}}</span>
    {{#if next}}
        <a href="{{page_url next}}"><i class="fa fa-chevron-right"></i></a>
    {{/if}}
</nav>